apiVersion: v1
kind: Service

metadata:
  name: demo-ui-service

spec:
  selector:
    app: demo-ui

  ports:
    - port: 80
      targetPort: 80
      nodePort: 32250

  type: NodePort

